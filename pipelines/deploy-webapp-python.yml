trigger:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-latest

steps:
  - checkout: self
  - script: |
      echo "Placeholder pipeline after repo reset"
      echo "No Python webapp source exists in this repository snapshot."
    displayName: 'Placeholder job'
